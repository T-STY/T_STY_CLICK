{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Emulator-backed security-rules tests for arthemis-f2966 (shared by client + admin apps).",
  "scripts": {
    "test": "mocha --timeout 15000 \"test/**/*.test.js\"",
    "test:emulator": "cd .. && firebase emulators:exec --only firestore --project arthemis-f2966 \"node firestore-tests/node_modules/mocha/bin/mocha.js --timeout 15000 firestore-tests/test/rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.0",
    "mocha": "^10.7.3"
  }
}
